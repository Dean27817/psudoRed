coppy:
 values: 2 

 command:
  -mov x y
move:
 values: 2

 command:
  -mov 0 x
  -dat 0 -1

int:
 values: 1 

 command:
   -spl  1
   -dat x

split:
 values: 1

 command:
  -spl  x

if:

while:

for:

fun:


