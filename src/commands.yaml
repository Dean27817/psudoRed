title: commands

coppy:
 -values:
  -x
  -y

 -command:
  -mov x y
move:
 -values:
  -x
  -y

 command:
  -mov 0 x
  -dat 0 -1

int:
 -values:
   -x

 -command:
   -spl  1
   -dat x

split:
 -values:
  -x

 -command:
  -spl  x

if:

while:

for:

fun:


